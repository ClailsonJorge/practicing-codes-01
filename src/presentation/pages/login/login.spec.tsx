import React from 'react';
import { render } from '@testing-library/react';
import Login from '@/presentation/pages/login/loginTest';

describe('Login Component', () => {
  test('', () => {
    render(<Login />);
  });
});
